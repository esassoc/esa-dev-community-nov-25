# Team 1 (Game Service - Spawning objects and Game State):

Rules: Only modify code in the `game-service` directory.

MVP:
Implement `startGame()`
Add one object to `gameObjects` signal every second
Implement `clickedGameObject()` method, which removes the clicked object from `gameObjects` and updates `gameStats` accordingly.
Auto-remove objects after 3 seconds.

Bonus:
Implement `stopGame()` and `resetGame()` methods
